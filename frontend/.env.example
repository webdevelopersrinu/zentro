# Copy to .env for local dev.

# Base URL of the backend API.
#   local dev (backend on :4000):  http://localhost:4000/api
#   production (same domain):       /api
VITE_API_URL=http://localhost:4000/api
